language: php

php:
  - 7.1
  - 7.2

before_script:
  - composer install

script:
  - composer test

after_script:
  - php vendor/bin/test-reporter --coverage-report=build/clover.xml

before_install:
  - composer self-update
